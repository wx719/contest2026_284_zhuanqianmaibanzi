# VelaPoka 软件架构与适配规划

## 系统总览

VelaPoka 是面向电子装配场景的固定工位视觉检查终端，基于 ESP32-P4X-Function-EV-Board。
系统通过 MIPI-CSI 摄像头采集画面，在 PSRAM 中进行图像比对，结果在 MIPI-DSI 触摸屏上显示，
检测记录存储在 MicroSD 卡，可通过以太网导出。

## 硬件资源映射（原理图 V1.8）

| 外设         | ESP32-P4 引脚            | 接口类型      | 当前状态   |
|-------------|-------------------------|--------------|-----------|
| UART0       | GPIO37(TX)/GPIO38(RX)   | UART 115200  | ✅ 已适配  |
| LCD Reset   | GPIO27                  | GPIO Out     | ✅ 已适配  |
| LCD Backlight| GPIO26                 | GPIO Out     | ✅ 已适配  |
| PHY Reset   | GPIO51                  | GPIO Out     | ✅ 已适配  |
| I2C (GT911) | GPIO7(SDA)/GPIO8(SCL)   | SW I2C 400K  | ✅ 已适配  |
| MIPI-DSI    | 专用差分对 (2 lane)      | DSI 1Gbps    | ⬜ 待适配  |
| MIPI-CSI    | 专用差分对 (2 lane)      | CSI          | ⬜ 待适配  |
| SDMMC1      | GPIO39-44               | SDIO 4-bit   | ⬜ 待适配  |
| SDMMC2      | GPIO45-50               | SDIO 4-bit   | ⬜ 待适配  |
| Ethernet    | GPIO34,49,50,28-31,52   | RMII         | ✅ 已适配  |
| MDC/MDIO    | GPIO31/52               | MDIO         | ✅ 已适配  |
| PSRAM       | MSPI 专用               | Octal SPI    | ✅ 已适配  |
| LDO CH3     | 内部                    | 2.5V (DSI)   | ⬜ 待适配  |
| LDO CH4     | 内部                    | VDDIO (SD)   | ⬜ 待适配  |
| WiFi/BT     | ESP32-C6-MINI-1         | UART         | 🔄 后期   |
| Audio       | I2S + I2C               | ES8311       | 🔄 可选   |

## 软件模块架构

```
┌──────────────────────────────────────────────────────────────────┐
│                        应用层 (app/)                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────┐   │
│  │ 样本录入  │  │ 装配检查  │  │ 位置校准  │  │ 质量追溯查询   │   │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └───────┬────────┘   │
│       └─────────────┼────────────┼─────────────────┘             │
│                     ▼                                            │
│  ┌──────────────────────────────────────────────────────────┐    │
│  │           装配状态机 (velapoka_assembly.c)                 │    │
│  │  IDLE → CAPTURE → CALIBRATE → INSPECT → RESULT → LOG     │    │
│  └────────────────────────┬─────────────────────────────────┘    │
├───────────────────────────┼──────────────────────────────────────┤
│                       服务层                                      │
│  ┌─────────────┐  ┌──────────────┐  ┌────────────────────────┐  │
│  │ 视觉检测引擎  │  │ 数据存储管理  │  │ 网络服务(以太网查询)   │  │
│  │ (inspect.c)  │  │ (storage.c)  │  │ (eth_service.c)       │  │
│  └──────┬──────┘  └──────┬───────┘  └────────┬───────────────┘  │
├─────────┼────────────────┼───────────────────┼───────────────────┤
│                      驱动层                                       │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────┐ │
│  │MIPI-DSI │ │MIPI-CSI │ │ SDMMC   │ │Ethernet │ │   PSRAM   │ │
│  │LCD Frame│ │V4L2 Cap │ │MMC/SD   │ │netdev   │ │heap集成    │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────────┘ │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │GT911    │ │控制GPIO  │ │I2C      │ │LDO CH3  │              │
│  │(已有)    │ │(已有)    │ │(已有)    │ │(DSI供电) │              │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │
├──────────────────────────────────────────────────────────────────┤
│  ESP32-P4 HAL (esp-hal-3rdparty) + NuttX Kernel                 │
└──────────────────────────────────────────────────────────────────┘
```

## 适配路线图

### Phase 1: PSRAM 使能 ✅
- 目标：启用外部 PSRAM，为图像帧缓冲提供大内存
- 工作：defconfig 添加 SPIRAM 配置
- 验收：`cat /dev/velapoka` 显示 extern_ram_seg 有使用量

### Phase 2: MIPI-DSI + LCD Framebuffer
- 目标：7 英寸 1024x600 显示输出
- 工作：
  - LDO CH3 2.5V 初始化（DSI 供电）
  - MIPI-DSI host 初始化（2 lane, 1Gbps）
  - LCD panel 初始化（复位序列 + DCS 命令）
  - 注册 NuttX framebuffer `/dev/fb0`
- 验收：`fb` 测试程序在屏幕上画图

### Phase 3: MIPI-CSI + Camera
- 目标：2MP 摄像头图像采集
- 工作：
  - CSI host 初始化（2 lane）
  - Camera sensor I2C 配置
  - DMA 帧缓冲管理（双缓冲，PSRAM 分配）
  - 注册 V4L2 设备 `/dev/video0`
- 验收：`v4l2` 工具捕获一帧图像

### Phase 4: SDMMC + 文件系统
- 目标：MicroSD 卡读写
- 工作：
  - SDMMC1 host 初始化（GPIO39-44）
  - LDO CH4 VDDIO 初始化
  - SD 卡探测 + FAT 文件系统挂载 `/mnt/sdcard`
- 验收：文件读写测试通过

### Phase 5: Ethernet ✅
- 目标：10/100Mbps 有线网络
- 工作：
  - RMII 引脚配置
  - IP101GRI PHY 初始化 + 复位序列
  - NuttX netdev 注册
  - DHCP / 静态 IP 配置
- 验收：ping 测试通过

实板验收记录（2026-08-24）：

- PHY 地址 1，复位 GPIO51；MAC 地址从 eFuse 正常读取。
- NuttX `eth0` 注册成功，链路状态为 `RUNNING`。
- 静态 IPv4：开发板 `10.0.0.2/24`，Windows 主机 `10.0.0.1/24`。
- 开发板到主机 Ping 10/10、主机到开发板 Ping 4/4，双向均为 0% 丢包。
- 清理移植期诊断日志后重新构建、烧录并完成相同双向 Ping 回归。
- DHCP、吞吐量和长时间稳定性尚未作为本阶段验收结论。

### Phase 6: 应用层
- 目标：完整视觉装配检查功能
- 工作：
  - 视觉检测算法（灰度差异、边缘、模板匹配）
  - 装配状态机（4 步防错流程）
  - UI 界面（触摸屏交互）
  - 异常记录与以太网导出

## 关键设计决策

1. **PSRAM 帧缓冲策略**：图像帧分配在 PSRAM 中（malloc 自动分配），内部 SRAM 保留给栈和 DMA 描述符
2. **双缓冲采集**：CSI 使用 ping-pong 双缓冲，一帧在处理时另一帧在采集
3. **检测算法选择**：灰度差异（快）→ 边缘匹配（中）→ 模板相似度（精），按需级联
4. **存储格式**：样本图片用 BMP（无压缩，读写快），检测记录用 JSON Lines
5. **网络协议**：以太网提供 HTTP REST API 查询记录，后期可加 WebSocket 实时推送
